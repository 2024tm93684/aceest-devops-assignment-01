apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-service
spec:
  type: NodePort
  selector:
    app: aceest-fitness
    slot: blue    # ← change to 'green' to switch traffic
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080