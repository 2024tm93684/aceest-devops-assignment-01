apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-service
spec:
  type: NodePort
  selector:
    app: aceest-fitness
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080   # access via http://$(minikube ip):30080